import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    textRedirect: {
        marginTop: 20,
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'flex-end'
    },
    icon: {
        width: 32,
        height: 32
    },
    loading: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    }
})